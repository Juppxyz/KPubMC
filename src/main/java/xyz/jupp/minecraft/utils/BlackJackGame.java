package xyz.jupp.minecraft.utils;


import java.util.ArrayList;
import java.util.List;

public class BlackJackGame {
    /*
    // Static variable to hold the deck of cards
    // Static variable to hold the deck of cards
    static List<String> deck = new ArrayList<>();
    // Arrays for dealer and player hands
    static List<String> playerHand = new ArrayList<>();
    static List<String> dealerHand = new ArrayList<>();

    public static void main(String[] args) {
        String[] values = {"Ace", "2", "3", "4", "5", "6", "7", "8", "9", "10", "Jack", "Queen", "King"};
        int index = 0;

        for (String value : values) {
            for (int i = 0; i < 4; i++) { // Each value appears 4 times
                deck[index] = value;
                index++;
            }
        }

        // Print the deck to verify
        for (String card : deck) {
            System.out.println(card);
        }
    }

    // Function to draw a card for the player
    public static String drawCardPlayer() {
        if (deck.isEmpty()) {
            return "Deck is empty!";
        }

        String card = deck.remove(0);
        playerHand.add(card);
        boolean isAce = card.equals("Ace");
        return "Player drew: " + card + ", isAce: " + isAce;
    }

    // Function to draw a card for the dealer
    public static String drawCardDealer() {
        if (deck.isEmpty()) {
            return "Deck is empty!";
        }

        String card = deck.remove(0);
        dealerHand.add(card);
        boolean isAce = card.equals("Ace");
        return "Dealer drew: " + card + ", isAce: " + isAce;
    }
    */

}
